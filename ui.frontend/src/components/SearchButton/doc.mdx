# SearchButton

import { Story } from '@storybook/addon-docs';

Componente que realiza a busca do input inserido no componente "InputSearch"

## Propriedades

### Text

Texto renderizado no Botão

padrão: `Buscar`

### fontSize

tamanho da fonte exibida no botão

padrão: `18`
