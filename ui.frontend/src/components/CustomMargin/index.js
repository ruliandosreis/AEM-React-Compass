import React from 'react'
import { CustomMarginContainer } from './style'

const CustomMargin = ({margin}) => <CustomMarginContainer margin={`${margin}px`}></CustomMarginContainer>

export default CustomMargin